<!DOCTYPE html>
<html>
  <head>
    <title>SubscriptionTracker</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
   
  </head>

  <body>
      <h1>Subscription Tracker</h1>
        <% if logged_in? %>             
            <%= button_to 'Logout', logout_path %>               
        <% end %>

    <%= yield %>
  </body>
</html>
